<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:OpenCvFilterMaker2">

    <!-- ColorNoiseRemovalFilter用テンプレート -->
    <DataTemplate DataType="{x:Type local:ColorNoiseRemovalFilter}">
        <StackPanel Margin="10">
            <TextBlock Text="ColorNoiseRemovalFilter Settings"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="KernelSize:"
                            VerticalAlignment="Center"
                            Margin="0,0,5,0"/>
                <TextBox Width="60"
                            Text="{Binding KernelSize.Value}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ThresholdValue:"
                            VerticalAlignment="Center"
                            Margin="0,0,5,0"/>
                <TextBox Width="60"
                            IsEnabled="{Binding IsManual.Value}"
                            Text="{Binding ThresholdValue.Value}"/>
                <CheckBox Content="Auto"
                     IsChecked="{Binding IsAuto.Value}"
                     Margin="0,0,0,5"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="DiffValue:"
                            VerticalAlignment="Center"
                            Margin="0,0,5,0"/>
                <TextBox Width="60"
                            Text="{Binding DiffSize.Value}"/>
            </StackPanel>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>