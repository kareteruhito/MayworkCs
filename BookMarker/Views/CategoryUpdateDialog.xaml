<Window x:Class="BookMarker.Views.CategoryUpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="カテゴリ名の変更"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel 
            Grid.Row="0"
            Orientation="Horizontal"
            Margin="0,12,0,0">
            <TextBlock
                Width="100"
                Text="変更前:" />
            <TextBlock
                Text="{Binding OldCategory}" />
        </StackPanel>

        <StackPanel 
            Grid.Row="1"
            Orientation="Horizontal"
            Margin="0,12,0,0">
            <TextBlock
                Width="100"
                Text="変更後:" />
            <TextBox
                x:Name="NameTextBox"
                Margin="0,10,0,0"
                MinWidth="360"
                Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,12,0,0">
                <Button
                    Content="OK"
                    Width="90"
                    IsDefault="True"
                    Margin="0,0,8,0"
                    Click="Ok_Click" />
                <Button
                    Content="キャンセル"
                    Width="90"
                    IsCancel="True"
                    Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>